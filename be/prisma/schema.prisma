generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("PRISMA_URL")
}

model User {
  id Int  @default(autoincrement()) @id
  email String
  hashedPassword String
}
